# Shared Library

This library contains common utilities, types, and interfaces used across all domains.

## What's Inside

- **Types**: Common types like `Result<T, E>` for error handling
- **Interfaces**: Base interfaces like `IRepository<T, ID>`

## Purpose

- Reduce code duplication across domains
- Provide consistent patterns
- Shared utilities that don't belong to any specific domain
