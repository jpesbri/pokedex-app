{
  "name": "trainer-adapters",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/trainer/adapters/src",
  "projectType": "library",
  "tags": ["type:adapters", "scope:trainer"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/trainer/adapters/jest.config.ts"
      }
    }
  }
}
